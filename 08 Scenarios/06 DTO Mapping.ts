/*
In this scenario we practice DTO mapping between layers.

We will:
- define separate DTOs for API and domain
- map between external and internal models
- keep layers decoupled with TypeScript types
- apply best practices used in production systems
*/
