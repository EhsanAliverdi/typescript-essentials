/*
In this section we will learn how to debug TypeScript code.

We will learn:
- using breakpoints
- source maps
- debugging in VS Code
*/

// Example code
function add(a: number, b: number) {
  return a + b;
}

const result = add(2, 3);

console.log(result);

// Put breakpoint on "return a + b"
// Run debugger in VSCode
