/*
In this section, we will configure the TypeScript compiler to ensure that our project is set up correctly.
 We will create a `tsconfig.json` file that specifies the compiler options and the files to include in our project.
*/

//to do this we can run the following command in the terminal:
// tsc --init
// This will create a `tsconfig.json` file in the root of our project. 
// We can then edit this file to specify our compiler options and the files to include in our project.

//in Generated config file we can configure the compiler options 
// for example, we can set the target version of JavaScript to ESNext and the module system to NodeNext as follows:
// or we can specify output directory or input directory
